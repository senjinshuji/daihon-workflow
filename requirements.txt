# BB-Agent Requirements
# Python 3.8+ required

# Core dependencies for the BB-Agent system
# Most functionality uses built-in Python libraries

# Optional dependencies for enhanced functionality
# Uncomment if needed:

# For CSV processing (built-in csv module is used)
# pandas>=1.3.0

# For JSON handling (built-in json module is used)
# ujson>=4.0.0

# For subprocess management (built-in subprocess module is used)
# psutil>=5.8.0

# For datetime handling (built-in datetime module is used)
# python-dateutil>=2.8.0

# For regex operations (built-in re module is used)  
# regex>=2021.0.0

# For file operations (built-in os, glob modules are used)
# pathlib2>=2.3.0

# System requirements:
# - Python 3.8+
# - Claude CLI (https://docs.anthropic.com/claude/docs/cli-installation)
# - tmux (brew install tmux on macOS)
# - bash shell
# - git